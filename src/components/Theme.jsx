import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  typeography: {
    allVariants: {
      color: '#FFFFFF'
    }
  },

  palette: {
    primary: {
      main: '#50DEDF'
    },
    secondary: {
      main: '#FFFFFF'
    }
  }
})

export default theme;
